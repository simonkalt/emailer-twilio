[dev]
	command = "yarn dev:server" # Command to start your dev server
	port = 443 # Port that the dev server will be listening on
	publish = "public" # If you use a _redirect file, provide the path to your static content folder’
    targetPort = 4433
    framework="#custom"